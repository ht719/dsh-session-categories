# `@deepseek-ai/dsh-session-categories-bundle`

English | [中文](README.zh.md)

This development-only bundle predates the self-contained `@deepseek-ai/dsh-session-categories` package. End users install the single package documented there; this package remains only for source-level composition tests.

It composes the former Host and Client development rows through [`cordis.patch.yml`](cordis.patch.yml). Categories remain available on the Web profile only.
